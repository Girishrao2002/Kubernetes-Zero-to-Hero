apiVersion: v1
kind: Service
metadata:
  name: demo-nodeport
spec:
  type: NodePort
  selector:
    app: service-demo
    service-type: nodeport
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
